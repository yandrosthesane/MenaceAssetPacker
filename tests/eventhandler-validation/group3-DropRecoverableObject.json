{
  "name": "EventHandler - DropRecoverableObject",
  "description": "Verify DropRecoverableObject EventHandler loads and can be read. Drops recoverable items on death.",
  "steps": [
    {
      "type": "command",
      "command": "test.goto_main"
    },
    {
      "type": "wait",
      "durationMs": 2000
    },
    {
      "type": "repl",
      "name": "Find skill with DropRecoverableObject EventHandler",
      "code": "var skills = Menace.SDK.Templates.FindAll(\"SkillTemplate\"); var skillFound = null; foreach(var skill in skills) { var handlers = (System.Collections.IList)Menace.SDK.Templates.GetProperty(\"SkillTemplate\", skill.name, \"EventHandlers\"); if(handlers != null) { for(int i = 0; i < handlers.Count; i++) { var hType = Menace.SDK.Templates.GetProperty(\"SkillTemplate\", skill.name + \"|EventHandlers[\" + i + \"]\", \"_type\"); if(hType != null && hType.ToString().Contains(\"DropRecoverableObject\")) { skillFound = skill.name; break; } } } if(skillFound != null) break; } skillFound != null && !skillFound.IsEmpty"
    },
    {
      "type": "repl",
      "name": "Load EventHandlers array from found skill",
      "code": "var handlers = (System.Collections.IList)Menace.SDK.Templates.GetProperty(\"SkillTemplate\", \"effect.volatile_explosive\", \"EventHandlers\"); handlers != null && handlers.Count > 0"
    },
    {
      "type": "repl",
      "name": "Verify _type field is DropRecoverableObject",
      "code": "var typeField = Menace.SDK.Templates.GetProperty(\"SkillTemplate\", \"effect.volatile_explosive|EventHandlers[0]\", \"_type\"); typeField != null && typeField.ToString().Contains(\"DropRecoverableObject\")"
    },
    {
      "type": "repl",
      "name": "Read ObjectTileEffect parameter",
      "code": "var objEffect = Menace.SDK.Templates.GetProperty(\"SkillTemplate\", \"effect.volatile_explosive|EventHandlers[0]\", \"ObjectTileEffect\"); objEffect != null && objEffect.ToString().Length > 0"
    },
    {
      "type": "repl",
      "name": "Read ObjectUnitHUDIcon parameter",
      "code": "var hudIcon = Menace.SDK.Templates.GetProperty(\"SkillTemplate\", \"effect.volatile_explosive|EventHandlers[0]\", \"ObjectUnitHUDIcon\"); hudIcon != null"
    }
  ]
}
