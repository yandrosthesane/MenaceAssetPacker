{
  "name": "EventHandler - FilterByCondition",
  "description": "Verify FilterByCondition EventHandler loads and can be read. Filters skill/perk by tactical conditions.",
  "steps": [
    {
      "type": "command",
      "command": "test.goto_main"
    },
    {
      "type": "wait",
      "durationMs": 2000
    },
    {
      "type": "repl",
      "name": "Find skill with FilterByCondition EventHandler",
      "code": "var skills = Menace.SDK.Templates.FindAll(\"SkillTemplate\"); var skillFound = null; foreach(var skill in skills) { var handlers = (System.Collections.IList)Menace.SDK.Templates.GetProperty(\"SkillTemplate\", skill.name, \"EventHandlers\"); if(handlers != null) { for(int i = 0; i < handlers.Count; i++) { var hType = Menace.SDK.Templates.GetProperty(\"SkillTemplate\", skill.name + \"|EventHandlers[\" + i + \"]\", \"_type\"); if(hType != null && hType.ToString().Contains(\"FilterByCondition\")) { skillFound = skill.name; break; } } } if(skillFound != null) break; } skillFound != null && !skillFound.IsEmpty"
    },
    {
      "type": "repl",
      "name": "Load EventHandlers array from found skill",
      "code": "var handlers = (System.Collections.IList)Menace.SDK.Templates.GetProperty(\"SkillTemplate\", \"active.brace_for_impact\", \"EventHandlers\"); handlers != null && handlers.Count > 0"
    },
    {
      "type": "repl",
      "name": "Verify _type field is FilterByCondition",
      "code": "var typeField = Menace.SDK.Templates.GetProperty(\"SkillTemplate\", \"active.brace_for_impact|EventHandlers[0]\", \"_type\"); typeField != null && typeField.ToString().Contains(\"FilterByCondition\")"
    },
    {
      "type": "repl",
      "name": "Read Condition parameter",
      "code": "var condition = Menace.SDK.Templates.GetProperty(\"SkillTemplate\", \"active.brace_for_impact|EventHandlers[0]\", \"Condition\"); condition != null && condition.ToString().Length > 0"
    }
  ]
}
