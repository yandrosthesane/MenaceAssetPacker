{
  "name": "EventHandler - DisableItem",
  "description": "Verify DisableItem EventHandler loads and can be read. Disables items based on tags and logic.",
  "steps": [
    {
      "type": "command",
      "command": "test.goto_main"
    },
    {
      "type": "wait",
      "durationMs": 2000
    },
    {
      "type": "repl",
      "name": "Find skill with DisableItem EventHandler",
      "code": "var skills = Menace.SDK.Templates.FindAll(\"SkillTemplate\"); var skillFound = null; foreach(var skill in skills) { var handlers = (System.Collections.IList)Menace.SDK.Templates.GetProperty(\"SkillTemplate\", skill.name, \"EventHandlers\"); if(handlers != null) { for(int i = 0; i < handlers.Count; i++) { var hType = Menace.SDK.Templates.GetProperty(\"SkillTemplate\", skill.name + \"|EventHandlers[\" + i + \"]\", \"_type\"); if(hType != null && hType.ToString().Contains(\"DisableItem\")) { skillFound = skill.name; break; } } } if(skillFound != null) break; } skillFound != null && !skillFound.IsEmpty"
    },
    {
      "type": "repl",
      "name": "Load EventHandlers array from found skill",
      "code": "var handlers = (System.Collections.IList)Menace.SDK.Templates.GetProperty(\"SkillTemplate\", \"effect.tethered\", \"EventHandlers\"); handlers != null && handlers.Count > 0"
    },
    {
      "type": "repl",
      "name": "Verify _type field is DisableItem",
      "code": "var typeField = Menace.SDK.Templates.GetProperty(\"SkillTemplate\", \"effect.tethered|EventHandlers[0]\", \"_type\"); typeField != null && typeField.ToString().Contains(\"DisableItem\")"
    },
    {
      "type": "repl",
      "name": "Read RequiredLogic parameter",
      "code": "var reqLogic = Menace.SDK.Templates.GetProperty(\"SkillTemplate\", \"effect.tethered|EventHandlers[0]\", \"RequiredLogic\"); reqLogic != null"
    },
    {
      "type": "repl",
      "name": "Read RequiredTags parameter",
      "code": "var reqTags = Menace.SDK.Templates.GetProperty(\"SkillTemplate\", \"effect.tethered|EventHandlers[0]\", \"RequiredTags\"); reqTags != null"
    }
  ]
}
