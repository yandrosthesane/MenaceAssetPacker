{
  "name": "EventHandler - GrantBonusTurn",
  "description": "Verify GrantBonusTurn EventHandler loads and can be read. Grants bonus turn to entity.",
  "steps": [
    {
      "type": "command",
      "command": "test.goto_main"
    },
    {
      "type": "wait",
      "durationMs": 2000
    },
    {
      "type": "repl",
      "name": "Find skill with GrantBonusTurn EventHandler",
      "code": "var skills = Menace.SDK.Templates.FindAll(\"SkillTemplate\"); var skillFound = null; foreach(var skill in skills) { var handlers = (System.Collections.IList)Menace.SDK.Templates.GetProperty(\"SkillTemplate\", skill.name, \"EventHandlers\"); if(handlers != null) { for(int i = 0; i < handlers.Count; i++) { var hType = Menace.SDK.Templates.GetProperty(\"SkillTemplate\", skill.name + \"|EventHandlers[\" + i + \"]\", \"_type\"); if(hType != null && hType.ToString().Contains(\"GrantBonusTurn\")) { skillFound = skill.name; break; } } } if(skillFound != null) break; } skillFound != null && !skillFound.IsEmpty"
    },
    {
      "type": "repl",
      "name": "Load EventHandlers array from found skill",
      "code": "var handlers = (System.Collections.IList)Menace.SDK.Templates.GetProperty(\"SkillTemplate\", \"effect.blaze_of_glory\", \"EventHandlers\"); handlers != null && handlers.Count > 0"
    },
    {
      "type": "repl",
      "name": "Verify _type field is GrantBonusTurn",
      "code": "var typeField = Menace.SDK.Templates.GetProperty(\"SkillTemplate\", \"effect.blaze_of_glory|EventHandlers[0]\", \"_type\"); typeField != null && typeField.ToString().Contains(\"GrantBonusTurn\")"
    },
    {
      "type": "repl",
      "name": "Read ActionPointsAvailable parameter",
      "code": "var apAvail = Menace.SDK.Templates.GetProperty(\"SkillTemplate\", \"effect.blaze_of_glory|EventHandlers[0]\", \"ActionPointsAvailable\"); apAvail != null && (double)apAvail >= 0"
    }
  ]
}
